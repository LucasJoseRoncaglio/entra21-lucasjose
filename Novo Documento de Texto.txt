function casoOK(linha) {
                let outroNome = prompt("digite o nome correto")
                matriz[linha][1] = outroNome
            }   
            function verificacaoNome() {
                let verificaMatricula = +prompt("insira o número da matriculo")
                for (let i = 0; i < 3; i++) {
                    for (let j = 0; j < 6; j++) {
                        if (verificaMatricula === matriz[i][j]) {
                            confirm(`O usuario ${matriz[i][1]} está cadastrado na matricula ${verificaMatricula}, deseja alterar? Pressione ok.`)
                            if (true) {
                                casoOK(i)
                            }
                        }
                    }
                }
            }






let c
        let matriz = []
        let notas = []
        function chamarMenu() {


            while (c != 0) {
                opcao = Number(prompt("Digite um Número Corespondente a opção\n 1-) Cadastrar Nome\n 2-) Editar Nome\n 3-)Pedir Nota do Aluno\n 4-)Exibição dos Dados em Tabela"))
                switch (opcao) {
                    case 1:
                        cadastraNome()
                        break;
                    case 2:
                        verificacaoNome()
                        break;
                    case 3:
                        calcularMedia()
                        pedeNotas()
                        cadastraNotas(matricula)

                        break;
                    case 4:
                        organizarTabela()
                        break
                    default:
                        alert("Opção inválida")



                }

                c = Number(prompt("Digite 1 Para Continuar ou 0 Para Sair"))
            }

        }
        chamarMenu()

        function cadastraNome() {

            for (let linha = 0; linha < 3; linha++) {
                matriz[linha] = []
                for (let coluna = 0; coluna < 2; coluna++) {


                    if (coluna === 0) {
                        matriz[linha][coluna] = +prompt("Digite a Matricula")
                    } else {
                        matriz[linha][1] = prompt("Digite o Nome")

                    }
                }

            }
        }

            function casoOK(linha) {
                let outroNome = prompt("digite o nome correto")
                matriz[linha][1] = outroNome
            }   
            function verificacaoNome() {
                let verificaMatricula = +prompt("insira o número da matriculo")
                for (let i = 0; i < 3; i++) {
                    for (let j = 0; j < 6; j++) {
                        if (verificaMatricula === matriz[i][j]) {
                            confirm(`O usuario ${matriz[i][1]} está cadastrado na matricula ${verificaMatricula}, deseja alterar? Pressione ok.`)
                            if (true) {
                                casoOK(i)
                            }
                        }
                    }
                }
            }

            function pedeNotas() {
            let nota1 = +prompt("insira nota 1")
            let nota2 = +prompt("insira nota 2")
            let nota3 = +prompt("insira nota 3")

            return [nota1, nota2, nota3]
        }
        

        function calcularMedia() {
            let media = (nota1 + nota2 + nota3) / 3
            return media
        }

        function cadastraNotas(matricula) {
            notas = pedeNotas()

            for (let i =0; i < matriz.length; i++) {
            matriz[i][0] === matricula
            matriz[i][2] = notas[0]
            matriz[i][3] = notas[1]
            matriz[i][4] = notas[2]
            matriz[i][5] = calcularMedia[3]
        }
        }